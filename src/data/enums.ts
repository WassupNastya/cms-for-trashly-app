export enum Root {
  Items = '/items',
  Rules = '/rules',
  Decisions = '/decisions',
  Locations = '/locations',
}

export enum Tab {
  Items = 'Item',
  Groups = "Group",
  Categories = "Category",
  Properties = "Property",
}

export enum Type {
  ForItem = 0,
  ForGroup,
  ForCategory,
  ForPropertiesOnly
}

export enum Collection {
  Items = 'items',
  Groups = 'groups',
  Categories = 'categories',
  Properties = 'properties',
  Rules = 'rules',
  Decisions = 'decisions',
  Locations = 'locations'
}
