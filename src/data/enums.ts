export enum Root {
  Items = '/items',
  Rules = '/rules',
  Decisions = '/decisions',
  Locations = '/locations',
}

export enum Tab {
  Items = 0,
  Groups,
  Categories,
  Properties,
}

export enum Type {
  ForItem = 0,
  ForGroup,
  ForCategory,
  ForPropertiesOnly
}
